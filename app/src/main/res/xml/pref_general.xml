<PreferenceScreen xmlns:android="http://schemas.android.com/apk/res/android">

    <EditTextPreference
        android:key="@string/ip_key"
        android:title="@string/pref_title_ip"
        android:defaultValue="@string/default_ip"
        android:selectAllOnFocus="true"
        android:inputType="number"
        android:capitalize="words"
        android:singleLine="true"
        android:maxLines="1" />

    <EditTextPreference
        android:key="@string/fragment_refresh_key"
        android:title="@string/pref_title_fragment_refresh"
        android:defaultValue="@string/default_fragment_refresh_rate"
        android:selectAllOnFocus="true"
        android:inputType="number"
        android:capitalize="words"
        android:singleLine="true"
        android:maxLines="1" />

</PreferenceScreen>
